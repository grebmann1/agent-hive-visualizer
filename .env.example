# Copy to `.env.local` and fill in. Next.js auto-loads `.env.local` for the
# dev server. Restart `npm run dev:web` after changes.

# Anthropic API key — used by the in-game NPC dialog (claude -p falls
# back if unset). https://console.anthropic.com → Settings → API keys
ANTHROPIC_API_KEY=
